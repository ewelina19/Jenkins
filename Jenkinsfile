pipeline {
    agent any
    stages {
        stage('Test') {
            echo "Test"
        }
        stage('Build') {
            echo "Build"
        }
        stage('Deploy (Staging)') {
            echo "Deploy (Staging"
        }
        stage('Manual Approval') {
            echo "Deploy (Staging"
        }
        stage('Deploy (Production)') {
            echo "Deploy (Production)"
        }   
    }
}
